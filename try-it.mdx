---
title: "Try SEC API"
description: "Run authenticated SEC API requests for entity resolution, filings, statements, SDKs, CLI, and MCP"
---

Use this page to make a small live request before you build a larger workflow. Resolve a company first, then use the returned identity to choose the filing, statement, or search request you need.

<Warning>
These examples call the production API and count against your usage quota. Start with the shown low limits while you validate response shapes.
</Warning>

## Set your API key

Create an account at [secapi.ai/signup](https://secapi.ai/signup), create an API key in the signed-in [dashboard](https://secapi.ai/app), and expose it only to the process making the request.

```bash
export SECAPI_API_KEY="secapi_..."
```

Send the key in `x-api-key`. Do not put it in browser code, a checked-in file, or an `Authorization: Bearer` header.

## Resolve a company

This request resolves Apple and returns its canonical CIK and identity metadata. Keep the request ID with your logs.

<CodeGroup>
```bash cURL
curl -sS \
  -H "x-api-key: $SECAPI_API_KEY" \
  "https://api.secapi.ai/v1/entities/resolve?ticker=AAPL&view=agent"
```

```javascript JavaScript
const response = await fetch(
  "https://api.secapi.ai/v1/entities/resolve?ticker=AAPL&view=agent",
  { headers: { "x-api-key": process.env.SECAPI_API_KEY } },
)

if (!response.ok) throw new Error(`SEC API request failed: ${response.status}`)
const entity = await response.json()
console.log(entity.ticker, entity.cik, entity.name, entity.requestId)
```

```python Python
import os
import requests

response = requests.get(
    "https://api.secapi.ai/v1/entities/resolve",
    params={"ticker": "AAPL", "view": "agent"},
    headers={"x-api-key": os.environ["SECAPI_API_KEY"]},
    timeout=30,
)
response.raise_for_status()
entity = response.json()
print(entity["ticker"], entity["cik"], entity["name"], entity["requestId"])
```

```bash CLI
secapi entities resolve --ticker AAPL
```
</CodeGroup>

On success, confirm that the result includes `ticker`, `cik`, `name`, and `requestId`. For the complete set of accepted identifiers and response fields, read the [entity resolution reference](/api-reference/entities/get-v1-entities-resolve).

## Retrieve the latest 10-K

```bash
curl -sS \
  -H "x-api-key: $SECAPI_API_KEY" \
  "https://api.secapi.ai/v1/filings/latest?ticker=AAPL&form=10-K&view=agent"
```

Keep `accessionNumber`, `filingDate`, `filingUrl`, and `requestId` with any result derived from the filing. The [latest filing reference](/api-reference/filings/get-v1-filings-latest) documents the available filters and response shape.

## Request annual statements

```bash
curl -sS \
  -H "x-api-key: $SECAPI_API_KEY" \
  "https://api.secapi.ai/v1/statements/all?ticker=AAPL&period=annual&limit=1"
```

This returns one normalized annual bundle containing a balance sheet, income statement, and cash flow statement. Set `period=quarterly` for quarterly results. The route accepts `limit` from 1 through 40.

## Explore further

<CardGroup cols={3}>
  <Card title="First request flows" icon="route" href="/first-request-flows">
    Add a filing section and compare REST, SDK, CLI, and MCP paths.
  </Card>
  <Card title="API playground" icon="square-terminal" href="/api-playground">
    Browse additional, copyable workflow examples.
  </Card>
  <Card title="API reference" icon="book-open" href="/api-reference">
    Check exact route requirements before changing an example.
  </Card>
</CardGroup>

Read [API conventions](/api-conventions) before adding pagination, version pinning, or retry logic. Read [Troubleshooting](/troubleshooting) when a request returns an error.
