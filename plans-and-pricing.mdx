---
title: "Plans and Pricing | SEC API"
description: "Compare SEC API plans — the free starter grant, Pay As You Go, Personal, Team, and Commercial — to pick the right pricing and rights for your usage."
---

# Plans and Pricing

## Default motion

- every new organization starts with `150` included calls, one-time (with an optional self-serve reset every 30 days)
- Pay As You Go is the default paid path
- Personal and Team are self-serve commit plans
- Commercial is the first plan with redistribution and external commercial rights

## Launch plans

<CardGroup cols={2}>
  <Card title="Pay As You Go" icon="gauge">
    No fixed monthly fee. Highest per-call pricing. Best for low-friction onboarding and real-world evaluation after the starter grant.
  </Card>
  <Card title="Personal" icon="user">
    `$55/mo`. Lower unit pricing for a single analyst or developer who has repeated usage.
  </Card>
  <Card title="Team" icon="users">
    `$239/mo`. Shared-org plan for up to five seats with higher throughput and more keys.
  </Card>
  <Card title="Commercial" icon="building-2">
    From `$18,000/yr`. Redistribution rights, Bulk Factor Export, contract handling, invoicing, and deeper onboarding.
  </Card>
</CardGroup>

## Meter-family launch pricing

- `light_reads`: `$0.01` — entity resolution and lightweight metadata
- `standard_reads`: `$0.02` — filings, facts, statements, ownership, compensation
- `heavy_extracts`: `$0.05` — section extraction and comparison workflows
- `artifact_jobs`: `$0.10` — artifact generation and export jobs
- `delivery_events`: `$0.001` — webhook and stream delivery
- `email_notifications`: `$0.005` — one billable recipient submitted for customer-configured monitor email notifications
- `intelligence_queries`: `$0.08` — semantic intelligence, country reports, portfolio analysis, strategy queries
- `market_data_reads`: `$0.03` — factor returns, macro indicators, market snapshots
- `semantic_search`: `$0.04` — vector-based semantic search across filing content

Email notifications are priced separately from webhook and stream delivery because the unit includes provider cost, retry infrastructure, unsubscribe handling, signed provider-feedback processing, support overhead, and margin. Personal receives a 20% meter discount (`$0.004`), Team receives 35% (`$0.00325`), and Commercial pricing must not go below `$0.0025` per submitted recipient without committed-volume approval.

## Monthly AI-query allowance

The `ai_queries` quota covers semantic intelligence + AI-graded routes (`/v1/intelligence/*` and `GET /v1/signals/dilution/enhanced`). Resets on the first day of each UTC calendar month.

| Plan | Monthly AI queries |
| --- | --- |
| Sandbox grant | 10 (trial) |
| Pay As You Go | Uncapped — metered per call |
| Personal | Uncapped — governed by budget gates |
| Team | Uncapped — governed by budget gates |
| Commercial | Uncapped — governed by budget gates |

Paid tiers are uncapped on `ai_queries`. Intelligence routes are LLM-free at request
time, so paid usage is bounded by your billing budget (a default soft alert at ~2× the
base fee and an abuse-protection hard cap at ~50× the base fee, both overridable via
`PUT /v1/billing/budget`) rather than a fixed query count. Only the sandbox grant
keeps a hard monthly trial cap.

Every quota-counted response carries `SECAPI-AI-Quota-Limit`, `SECAPI-AI-Quota-Used`, `SECAPI-AI-Quota-Remaining`, and `SECAPI-AI-Quota-Reset` headers. `GET /v1/billing` returns the same numbers under `monthlyQuotas.ai_queries`.

## Rights boundary

- evaluation: starter grant
- in-house use: PAYG, Personal, Team
- redistribution, embedding, white-label, resale, and Bulk Factor Export: Commercial

## Read next

<CardGroup cols={3}>
  <Card title="Auth and pricing" icon="key-round" href="/auth-and-pricing">
    Review the overall launch auth, billing, and first-utility posture.
  </Card>
  <Card title="Pay As You Go" icon="credit-card" href="/pay-as-you-go">
    See the PAYG activation flow and post-grant billing transition.
  </Card>
  <Card title="Agent billing" icon="bot" href="/agent-billing">
    Learn how agents should reason about billing before expensive loops.
  </Card>
</CardGroup>
