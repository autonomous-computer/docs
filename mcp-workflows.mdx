# Hosted MCP Workflows

<Info>
Hosted MCP lets coding agents call Datastream directly. That means less glue code, less local wrapper drift, and faster time from prompt to useful investor workflow.
</Info>

## Why use hosted MCP

<CardGroup cols={3}>
  <Card title="Less wrapper code" icon="cable">
    Your agent can call the Datastream tool surface directly instead of waiting on a custom wrapper layer.
  </Card>
  <Card title="Same workflows" icon="equal">
    REST and hosted MCP sit on top of the same underlying Datastream workflows.
  </Card>
  <Card title="Faster setup" icon="rocket">
    A working MCP install gets you to a useful issuer, filing, or section workflow quickly.
  </Card>
</CardGroup>

## Install metadata

```bash
curl https://api.secapi.ai/mcp
```

That payload includes:

- the canonical SSE transport URL
- auth metadata links
- install hints
- current tool and resource summaries

<Steps>
  <Step title="Fetch the install metadata">
    Confirm the hosted MCP endpoint is live and returning its transport and auth metadata.
  </Step>
  <Step title="Register the server in your client">
    Add the SSE endpoint to Claude Code, Cursor, or another MCP-aware client.
  </Step>
  <Step title="Exercise a real tool path">
    Resolve an issuer, fetch a filing, and extract a section before calling the setup done.
  </Step>
</Steps>

## Common clients

### Claude Code

```bash
claude mcp add --transport sse omni-datastream https://api.secapi.ai/mcp
```

### Cursor

```json
{
  "omni-datastream": {
    "url": "https://api.secapi.ai/mcp",
    "headers": {
      "x-api-key": "${OMNI_DATASTREAM_API_KEY}"
    }
  }
}
```

## Good first tools

- `entities.resolve`
- `filings.search`
- `filings.latest`
- `sections.get`
- `statements.get`

<Prompt>
Set up Omni Datastream hosted MCP in this MCP-aware client. Register the SSE endpoint, resolve AAPL, fetch the latest 10-K, extract Item 1A, and return the result with requestId, provenance, freshness, and materialization metadata intact.
</Prompt>

## Example tool call

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "tools/call",
  "params": {
    "name": "filings.latest",
    "arguments": {
      "ticker": "AAPL",
      "form": "10-K"
    }
  }
}
```

