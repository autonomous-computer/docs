# Hosted MCP Workflows

<Info>
Hosted MCP lets coding agents call Datastream directly. That means less glue code, less local wrapper drift, and faster time from prompt to useful investor workflow.
</Info>

## Why use hosted MCP

<CardGroup cols={3}>
  <Card title="Less wrapper code" icon="cable">
    Your agent can call the Datastream tool surface directly instead of waiting on a custom wrapper layer.
  </Card>
  <Card title="Same workflows" icon="equal">
    REST and hosted MCP sit on top of the same underlying Datastream workflows.
  </Card>
  <Card title="Faster setup" icon="rocket">
    A working MCP install gets you to a useful issuer, filing, or section workflow quickly.
  </Card>
</CardGroup>

## Install metadata

```bash
curl https://api.secapi.ai/mcp
```

That payload includes:

- the canonical streamable HTTP MCP transport URL
- auth metadata links
- sponsored bootstrap URLs for human token issuance and machine bootstrap
- install hints
- current tool and resource summaries

<Steps>
  <Step title="Fetch the install metadata">
    Confirm the hosted MCP endpoint is live and returning its transport and auth metadata.
  </Step>
  <Step title="Bootstrap the machine identity">
    Have a human operator mint a sponsor token first, then exchange it through `POST /v1/agent/bootstrap` before you start repeated MCP work.
  </Step>
  <Step title="Register the server in your client">
    Add the streamable HTTP MCP endpoint to Claude Code, Cursor, or another MCP-aware client.
  </Step>
  <Step title="Exercise a real tool path">
    Resolve an issuer, fetch a filing, and extract a section before calling the setup done.
  </Step>
</Steps>

## Common clients

### Claude Code

```bash
claude mcp add --transport streamable-http omni-datastream https://api.secapi.ai/mcp
```

### Cursor

```json
{
  "omni-datastream": {
    "url": "https://api.secapi.ai/mcp",
    "headers": {
      "x-api-key": "${OMNI_DATASTREAM_API_KEY}"
    }
  }
}
```

## Good first tools

- `entities.resolve`
- `filings.search`
- `filings.latest`
- `sections.get`
- `statements.get`
- `market.snapshots`
- `macro.indicators`
- `factors.returns`
- `portfolio.analyze`
- `intelligence.query`

## AI query quota

MCP `tools/call` requests for `intelligence.query` and `signals.dilution.enhanced.get` count toward the monthly `ai_queries` quota. Successful calls include a top-level `_omni.quota` envelope with projected post-call usage:

```json
{
  "jsonrpc": "2.0",
  "id": "quota_example",
  "result": {},
  "_omni": {
    "quota": {
      "family": "ai_queries",
      "mcpToolName": "intelligence.query",
      "limit": 600,
      "used": 42,
      "remaining": 558,
      "resetAt": "2026-05-01T00:00:00.000Z"
    }
  }
}
```

When the quota is exhausted, hosted MCP returns HTTP `429` with JSON-RPC error code `-32005` and `error.data.code: "ai_query_quota_exceeded"`. Treat `-32004` as retryable timeout and `-32005` as non-retryable until quota reset or plan upgrade. If a tool circuit is open, hosted MCP returns HTTP `503` with JSON-RPC error code `-32006` and `error.data.code: "mcp_tool_circuit_open"`; retry it only after availability backoff.

<Prompt>
Set up Omni Datastream hosted MCP in this MCP-aware client. Register the streamable HTTP MCP endpoint, resolve AAPL, fetch the latest 10-K, extract Item 1A, and return the result with requestId, provenance, freshness, and materialization metadata intact.
</Prompt>

## Example tool call

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "tools/call",
  "params": {
    "name": "intelligence.query",
    "arguments": {
      "query": "Decompose AAPL's last 6M return into factors, macro drivers, filing-specific risks, then suggest hedges.",
      "entities": ["AAPL"],
      "lookback": "6m",
      "responseMode": "compact_json_and_md"
    }
  }
}
```

