# First Request Flows

## This page exists to get you to one verified success fast

Do not start broad. Get one path working end to end and keep the exact request and `Request-Id` from that success.

## API first request

```bash
curl "https://api.secapi.ai/healthz"

curl -H "x-api-key: $OMNI_DATASTREAM_API_KEY" \
  "https://api.secapi.ai/v1/entities/resolve?ticker=AAPL"

curl -H "x-api-key: $OMNI_DATASTREAM_API_KEY" \
  "https://api.secapi.ai/v1/statements/all?ticker=AAPL&period=annual&limit=1"
```

## CLI first request

```bash
omni-sec entities resolve --ticker AAPL --json
omni-sec filings latest --ticker AAPL --form 10-K --json
omni-sec sections get --ticker AAPL --form 10-K --section item_1a --json
```

## MCP first request

- call `entities.resolve`
- call `filings.latest`
- call `sections.get`

## Keep this support packet

- interface used
- endpoint or tool
- `Request-Id`
- expected result
- actual result
