---
title: "SEC Semantic Search API"
description: "Search SEC filing sections by concept, keep the citation fields, and verify the returned passage in the source filing."
---

Semantic search finds related disclosure language when exact words may vary. It is retrieval, not an opinion engine: a match is a reason to inspect a cited SEC filing, not proof of a risk, accounting treatment, or investment conclusion.

## Search by concept

```bash
curl -sS -H "x-api-key: $SECAPI_API_KEY" \
  "https://api.secapi.ai/v1/search/semantic?q=artificial%20intelligence%20regulatory%20risk&ticker=GOOGL&form=10-K&mode=hybrid&limit=5"
```

`q` is required. `mode` accepts `keyword`, `semantic`, or `hybrid`; it defaults to `hybrid`.

```json
{
  "object": "semantic_search",
  "query": "artificial intelligence regulatory risk",
  "mode": "hybrid",
  "sections": {
    "object": "list",
    "data": [
      {
        "accession": "0001652044-25-000014",
        "section_key": "item_1a",
        "source_url": "https://www.sec.gov/Archives/...",
        "ticker": "GOOGL",
        "char_start": 120,
        "char_end": 260,
        "highlighted_snippet": "..."
      }
    ],
    "count": 1,
    "degradedState": null
  },
  "requestId": "req_example"
}
```

## Verify the citation

1. Keep `accession`, `section_key`, and `source_url` with the result.
2. Retrieve the cited section when you need full context.
3. Treat `char_start` and `char_end` as offsets in section markdown, never raw SEC HTML.
4. If offsets are null or the response reports citation degradation, inspect the source rather than slicing text programmatically.

`view=agent` retains citation fields but removes `score` and `retrievalMode`. Use the default view if your application needs retrieval detail.

## Next steps

- [Full-text search](/seo/sec-full-text-search-api)
- [Section search](/api-reference/sections/get-v1-sections-search)
- [Semantic API reference](/api-reference/search/get-v1-search-semantic)
