# Coverage and historical depth

This page summarizes the current public coverage posture using the tracked corpus inventory artifacts.

## Current snapshot

As of `2026-03-16T07:49:42.753Z` the current tracked inventory reports:

- 47 seeded replay filings
- 12 issuers
- 3 seeded replay form families in the corpus inventory snapshot
- earliest filing date: `2025-05-05`
- latest filing date: `2026-03-13`

## Measurement coverage

The freshness dashboard adds broader measured-form coverage on top of the seeded replay inventory:

- 49 measured freshness targets
- 6 measured forms: `10-K`, `10-Q`, `8-K`, `4`, `DEF 14A`, and `13F-HR`
- 16 seeded historical paths with measured cold and warm section timings
- 100% historical section manifest coverage in the latest replay-backed dashboard

## Important caveat

The current corpus inventory artifact is still in `artifact_fallback` mode:

- `databaseConfigured: false`
- `databaseStatus: unavailable`
- observed scope: `seeded_replay_artifacts`

That means these coverage claims are intentionally scoped to the replay-backed truth we can verify from tracked artifacts in this environment, not a live production database census.

## Storage posture

The current storage lifecycle snapshot reports:

- hot policy target: 1 year
- warm policy target: 5 years
- cold policy target: 30 years
- current implementation stage: `seeded_artifact_only`
- current observed residency: 100% hot, 0% warm, 0% cold

## Source artifacts

- `ops/corpus-inventory/latest.json`
- `ops/freshness-dashboard/latest.json`
- `evals/artifacts/latest-replay-summary.json`
