# Freshness and trust

This page publishes the current freshness methodology and the latest tracked public trust claims.

## Measurement policy

The current freshness telemetry artifact uses:

- source timestamp: SEC `acceptanceDateTime`
- API availability: first successful Datastream `latestFiling` resolution in the measurement run
- normalization availability: completion of rendered filing normalization in the same run
- manifest availability: completion of section manifest derivation in the same run

The latest freshness artifact was generated at `2026-03-16T07:56:27.115Z`.

## Current measured forms

- `10-K`
- `10-Q`
- `8-K`
- `4`
- `DEF 14A`
- `13F-HR`

## Current public trust claims

These claims are copied directly from the tracked freshness dashboard artifact:

- `10-K` publish-to-Datastream lag p50=`3387618.72s`, p95=`19741477.01s`, sample count=`12`
- `10-Q` publish-to-Datastream lag p50=`11455838.34s`, p95=`12484417.71s`, sample count=`12`
- `13F-HR` publish-to-Datastream lag p50=`2285482.68s`, p95=`2310894.92s`, sample count=`2`
- `4` publish-to-Datastream lag p50=`394741.82s`, p95=`1516926.5s`, sample count=`6`
- `8-K` publish-to-Datastream lag p50=`1713317.2s`, p95=`4013521.51s`, sample count=`11`
- `DEF 14A` publish-to-Datastream lag p50=`26479523.21s`, p95=`29413062.35s`, sample count=`6`

## Freshness health checks

The latest dashboard also reports:

- freshness telemetry freshness: `ok`
- workflow benchmark freshness: `ok`
- replay summary freshness: `ok`
- Dagster confidence freshness: `ok`

## Source artifacts

- `ops/freshness-telemetry/latest.json`
- `ops/freshness-dashboard/latest.json`
- `benchmarks/dagster/results/latest.json`
