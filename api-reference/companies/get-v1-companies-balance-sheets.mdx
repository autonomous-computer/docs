# GET /v1/companies/balance-sheets

Return SEC XBRL-derived balance sheets for a ticker with normalized debt, equity, and cash fields

<Info>
Audience: application and coding agent.
</Info>

## Canonical metadata

- `requestId`
- `traceparent`

## Example request

<RequestExample>
```bash
curl -X GET \
  -H "x-api-key: $SEC_API_KEY" \
  -H "secapi-version: 2026-03-19" \
  "https://api.secapi.ai/v1/companies/balance-sheets"
```
</RequestExample>

## Example response

<ResponseExample>
```json
{
  "requestId": "req_example_123",
  "traceparent": "00-4bf92f3577b34da6a3ce929d0e0e4736-00f067aa0ba902b7-01"
}
```
</ResponseExample>

## Give this prompt to your agent

<Prompt>
Use SEC API GET /v1/companies/balance-sheets to return sec xbrl-derived balance sheets for a ticker with normalized debt, equity, and cash fields. Preserve `requestId`, `traceparent` in the output. Return a concise summary and preserve the structured fields your application needs for follow-up requests.
</Prompt>

## Errors and partial results

- treat non-2xx responses as contract-aware failures, not free-form errors
- preserve `requestId` and `traceparent` in logs and downstream reports
- if provenance or freshness metadata is present, return it unchanged so trust is not lost in the handoff


