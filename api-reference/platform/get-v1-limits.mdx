---
title: "GET /v1/limits"
description: "Return the current organization's effective plan, billing state, and route quota windows."
---

Return the current organization's effective plan, billing state, and route quota windows.

<Info>
Audience: application and coding agent.
</Info>

## Canonical metadata

- `requestId`
- `traceparent`
- `recordedAt`
- `effectivePlanKey`
- `billingState`
- `quotas`

## Example request

<RequestExample>
```bash
curl -X GET -H "x-api-key: $SECAPI_API_KEY" -H "secapi-version: 2026-03-19" "https://api.secapi.ai/v1/limits"
```
</RequestExample>

## Example response

<ResponseExample>
```json
{
  "object": "limits",
  "orgId": "org_example",
  "requestId": "req_2ZK8Q1W9F4M6P7R3",
  "recordedAt": "2026-03-19T12:00:00.000Z",
  "effectivePlanKey": "personal",
  "billingState": "active",
  "quotas": [{ "meterClass": "standard_read", "limit": 600, "period": "minute", "allowed": true, "planKey": "personal", "billingState": "active" }]
}
```
</ResponseExample>

## Authentication

This is an authenticated organization read. Send the same x-api-key used for the workflow you are inspecting.

## Give this prompt to your agent

<Prompt>
Use SEC API GET /v1/limits to return the current organization's effective plan, billing state, and route quota windows. Preserve `requestId`, `traceparent`, `recordedAt`, `effectivePlanKey`, `billingState`, `quotas` in the output. Return a concise markdown summary plus the exact structured payload fields that a downstream engineer or agent should keep using this result.
</Prompt>

## Failure posture

- treat non-2xx responses as contract-aware failures, not free-form errors
- preserve `requestId` and `traceparent` in logs and downstream reports
- if provenance or freshness metadata is present, return it unchanged so trust is not lost in the handoff
