# POST /v1/admin/maintenance/warm-market-data

Warm operator-scoped market snapshots, reference, estimates, bars, and corporate-action caches for the full dependency universe

<Info>
Audience: operator.
</Info>

## Canonical metadata

- `requestId`
- `traceparent`

## Example request

<RequestExample>
```bash
curl -X POST \
  -H "x-api-key: $OMNI_DATASTREAM_API_KEY" \
  -H "omni-version: 2026-03-19" \
  -H "content-type: application/json" \
  -d '{"refreshLive": false}' \
  "https://api.secapi.ai/v1/admin/maintenance/warm-market-data"
```
</RequestExample>

`refreshLive=false` runs the warm path against already-materialized market data instead of forcing a fresh provider pull. Use that for deterministic full-universe repair and factor backfill preparation.

## Example response

<ResponseExample>
```json
{
  "requestId": "req_example_123",
  "traceparent": "00-4bf92f3577b34da6a3ce929d0e0e4736-00f067aa0ba902b7-01"
}
```
</ResponseExample>

## Give this prompt to your agent

<Prompt>
Use Omni Datastream POST /v1/admin/maintenance/warm-market-data to warm operator-scoped market snapshots, reference, estimates, bars, and corporate-action caches for the full dependency universe used by factor materialization. Set `refreshLive=false` when you want a storage-backed repair or deterministic backfill-prep pass. Preserve `requestId`, `traceparent` in the output. Return a concise markdown summary plus the exact structured payload fields that a downstream engineer or agent should keep using this result.
</Prompt>

## Failure posture

- treat non-2xx responses as contract-aware failures, not free-form errors
- preserve `requestId` and `traceparent` in logs and downstream reports
- if provenance or freshness metadata is present, return it unchanged so trust is not lost in the handoff

