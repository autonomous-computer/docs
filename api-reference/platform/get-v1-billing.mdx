# GET /v1/billing

Return the current organization's billing snapshot, including pricing state, budget controls, and billing provider state

<Info>
Use this endpoint from server-side applications, SDKs, or agent workflows.
</Info>

## Response metadata

- `requestId`
- `traceparent`

## Example request

<RequestExample>
```bash
curl -X GET \
  -H "x-api-key: $SEC_API_KEY" \
  -H "secapi-version: 2026-03-19" \
  "https://api.secapi.ai/v1/billing"
```
</RequestExample>

## Example response

<ResponseExample>
```json
{
  "requestId": "req_example_123",
  "traceparent": "00-4bf92f3577b34da6a3ce929d0e0e4736-00f067aa0ba902b7-01",
  "publicPlanKey": "personal",
  "billingState": "personal_active",
  "monthlyQuotas": {
    "ai_queries": {
      "family": "ai_queries",
      "limit": 600,
      "used": 142,
      "remaining": 458,
      "resetAt": "2026-05-01T00:00:00.000Z",
      "graceUntil": null
    }
  }
}
```
</ResponseExample>

## Give this prompt to your agent

<Prompt description="Copy this SEC API prompt for your agent.">
Use SEC API GET /v1/billing to return the current organization's billing snapshot, including pricing state, budget controls, and billing provider state. Preserve `requestId`, `traceparent` in the output. Keep the response fields your application uses for follow-up requests.
</Prompt>

## Errors and partial results

- Treat non-2xx responses as API errors and log the response body for debugging
- Log `requestId` and `traceparent` so SEC API support can trace the request
- if provenance or freshness metadata is present, return it unchanged so trust is not lost in the handoff


