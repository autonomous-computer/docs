---
title: "POST /v1/admin/maintenance/ingest-worker"
description: "Run admin-scoped ingest worker passes to materialize queued filing artifacts and sections"
---

Run admin-scoped ingest worker passes to materialize queued filing artifacts and sections

<Info>
Audience: admin.
</Info>

## Canonical metadata

- `requestId`
- `traceparent`

## Example request

<RequestExample>
```bash
curl -X POST \
  -H "x-api-key: $SECAPI_API_KEY" \
  -H "secapi-version: 2026-03-19" \
  -H "content-type: application/json" \
  -d '{}' \
  "https://api.secapi.ai/v1/admin/maintenance/ingest-worker"
```
</RequestExample>

## Example response

<ResponseExample>
```json
{
  "requestId": "req_2ZK8Q1W9F4M6P7R3"
}
```
</ResponseExample>

## Give this prompt to your agent

<Prompt>
Use SEC API POST /v1/admin/maintenance/ingest-worker to run admin-scoped ingest worker passes to materialize queued filing artifacts and sections. Preserve `requestId`, `traceparent` in the output. Return a concise markdown summary plus the exact structured payload fields that a downstream engineer or agent should keep using this result.
</Prompt>

## Failure posture

- treat non-2xx responses as contract-aware failures, not free-form errors
- preserve `requestId` and `traceparent` in logs and downstream reports
- if provenance or freshness metadata is present, return it unchanged so trust is not lost in the handoff
