# GET /v1/filings/{accession_number}/sections/{section_key}

Extract a filing item or section from a specific accession-number filing

<Info>
Use this endpoint from server-side applications, SDKs, or agent workflows.
</Info>

## Agent mode

Pass `?view=agent` to receive citation fields and a snippet without the full `contentMd` section body. Use `mode=compact` when you still need `contentMd` but want it capped for token efficiency.

## Section mode

Use `mode=full` for the full extracted section body or `mode=compact` for capped `contentMd`. Invalid mode values return `400 invalid_section_mode` with `details.acceptedValues`.

## View values

Use `view=agent` for citation-ready snippets, `view=compact` for capped `contentMd`, or `view=default` for the default full response. Invalid view values return `400 invalid_query_parameter` with `details.acceptedValues`.

## Agent response fields

- `requestId`
- `object`
- `ticker`
- `key`
- `snippet`
- `accessionNumber`
- `accession`
- `section_key`
- `source_url`
- `startOffset`
- `endOffset`

## Example request

<RequestExample>
```bash
curl -X GET \
  -H "x-api-key: $SECAPI_API_KEY" \
  -H "secapi-version: 2026-03-19" \
  "https://api.secapi.ai/v1/filings/0000320193-25-000079/sections/item_1a?view=agent"
```
</RequestExample>

## Example response

<ResponseExample>
```json
{
  "object": "section",
  "ticker": "AAPL",
  "accessionNumber": "0000320193-25-000079",
  "key": "item_1a",
  "startOffset": null,
  "endOffset": null,
  "snippet": "The Company faces risks from macroeconomic conditions, supply constraints, platform policy changes, and competition.",
  "accession": "0000320193-25-000079",
  "section_key": "item_1a",
  "char_start": null,
  "char_end": null,
  "highlighted_snippet": null,
  "source_url": "https://www.sec.gov/Archives/edgar/data/320193/000032019325000079/aapl-20250927.htm",
  "requestId": "req_example_123"
}
```
</ResponseExample>

## Give this prompt to your agent

<Prompt description="Copy this SEC API prompt for your agent.">
Use SEC API GET /v1/filings/{accession_number}/sections/{section_key}?view=agent to extract a citation-ready section snippet from a specific filing. Preserve `requestId`, `ticker`, `key`, `snippet`, `accessionNumber`, `accession`, `section_key`, `source_url`, and any offset fields in the output.
</Prompt>

## Errors and partial results

- Treat non-2xx responses as API errors and log the response body for debugging
- invalid `mode` values return `invalid_section_mode` with `details.acceptedValues`
- invalid `view` values return `invalid_query_parameter` with `details.acceptedValues`
- Log `requestId`, `accessionNumber`, and `source_url` so SEC API support can trace the request
