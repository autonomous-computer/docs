# GET /v1/filings

Search filing manifests with historical filters, accession lookup semantics, sorting, and cursor pagination

<Info>
Audience: application and coding agent.
</Info>

## Issuer lookup

Use `ticker`, `symbol`, or `cik` to scope issuer searches. `symbol` is an alias for `ticker`; if both are provided, they must refer to the same issuer symbol.

## Canonical metadata

- `requestId`
- `traceparent`
- `provenance`
- `freshness`
- `materialization`

## Example request

<RequestExample>
```bash
curl -X GET \
  -H "x-api-key: $SECAPI_API_KEY" \
  -H "secapi-version: 2026-03-19" \
  "https://api.secapi.ai/v1/filings?symbol=AAPL&form=10-K&limit=1&view=agent"
```
</RequestExample>

## Example response

<ResponseExample>
```json
{
  "requestId": "req_2ZK8Q1W9F4M6P7R3",
  "provenance": {
    "source": "sec",
    "accessionNumber": "0000320193-25-000079",
    "filingUrl": "https://www.sec.gov/Archives/edgar/data/320193/000032019325000079/aapl-20250927_htm.xml",
    "retrievedAt": "2026-03-17T00:00:00.000Z",
    "parserVersion": "2026-03-13.5",
    "rawSourceKey": "0000320193-25-000079"
  },
  "freshness": {
    "status": "fresh",
    "asOf": "2026-03-17T00:00:00.000Z",
    "sourcePublishedAt": "2025-10-31T00:00:00.000Z",
    "lagMs": 900000
  },
  "materialization": {
    "parserVersion": "2026-03-13.5",
    "materializationVersion": "datastream-3.0"
  }
}
```
</ResponseExample>

## Give this prompt to your agent

<Prompt>
Use SEC API GET /v1/filings with `ticker`, `symbol`, or `cik` to search filing manifests with historical filters, accession lookup semantics, sorting, and cursor pagination. Preserve `requestId`, `traceparent`, `provenance`, `freshness`, `materialization` in the output. Return a concise markdown summary plus the exact structured payload fields that a downstream engineer or agent should keep using this result.
</Prompt>

## Failure posture

- treat non-2xx responses as contract-aware failures, not free-form errors
- if both `ticker` and `symbol` are present, they must match
- preserve `requestId` and `traceparent` in logs and downstream reports
- if provenance or freshness metadata is present, return it unchanged so trust is not lost in the handoff
