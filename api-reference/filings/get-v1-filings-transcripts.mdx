---
title: "GET /v1/filings/transcripts"
description: "Filing transcripts endpoint (currently unavailable)"
---

Filing transcripts endpoint (currently unavailable)

<Info>
Audience: application and coding agent.
</Info>

## Coverage and source scope

This endpoint is currently unavailable and returns `404 filings_intelligence_not_available`. When available, it will list persisted earnings-transcript records extracted from covered EX-99 filing materials, with speaker and prepared-remarks or Q&A structure where source material supports extraction. Results will depend on available source materials and extraction coverage.

## Current availability

This endpoint currently returns `404 filings_intelligence_not_available`. It is documented here so clients can prepare for the contract, but it is not available as a transcript source. When it becomes available, preserve filing or exhibit citations and verify material statements in the original source.

## Canonical metadata

- `requestId`
- `traceparent`
- `code`
- `message`
- `details`

## Example request

<RequestExample>
```bash
curl -X GET \
  -H "x-api-key: $SECAPI_API_KEY" \
  -H "secapi-version: 2026-03-19" \
  "https://api.secapi.ai/v1/filings/transcripts"
```
</RequestExample>

## 404 response

<ResponseExample>
```json
{
  "object": "error",
  "id": "err_example",
  "code": "filings_intelligence_not_available",
  "type": "invalid_request_error",
  "message": "The filings-intelligence plane is not enabled.",
  "requestId": "req_2ZK8Q1W9F4M6P7R3",
  "details": {}
}
```
</ResponseExample>

## Give this prompt to your agent

<Prompt>
GET /v1/filings/transcripts is currently unavailable and returns `404 filings_intelligence_not_available`. Do not use it as an active transcript source. When it becomes available, preserve filing or exhibit citations and verify material statements in the original source.
</Prompt>

## Failure posture

- treat `404 filings_intelligence_not_available` as the current endpoint state, not an empty result
- do not retry this endpoint as an active dependency until filings intelligence is enabled
- preserve `requestId` and `traceparent` in logs and downstream reports
