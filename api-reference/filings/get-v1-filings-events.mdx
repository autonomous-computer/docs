---
title: "GET /v1/filings/events"
description: "Filing events endpoint (currently unavailable)"
---

Filing events endpoint (currently unavailable)

<Info>
Audience: application and coding agent.
</Info>

## Coverage and source scope

This endpoint is currently unavailable and returns `404 filings_intelligence_not_available`. When available, it will list persisted classifications from covered 8-K and 6-K filings for issuer- and date-bounded monitoring. Rows are derived from filing content and are not a real-time market-news feed or a complete record of every issuer event.

## Current availability

This endpoint currently returns `404 filings_intelligence_not_available`. It is documented here so clients can prepare for the contract, but it is not available for active monitoring. When it becomes available, preserve filing citations and verify material events in the source filing.

## Canonical metadata

- `requestId`
- `traceparent`
- `code`
- `message`
- `details`

## Example request

<RequestExample>
```bash
curl -X GET \
  -H "x-api-key: $SECAPI_API_KEY" \
  -H "secapi-version: 2026-03-19" \
  "https://api.secapi.ai/v1/filings/events"
```
</RequestExample>

## 404 response

<ResponseExample>
```json
{
  "object": "error",
  "id": "err_example",
  "code": "filings_intelligence_not_available",
  "type": "invalid_request_error",
  "message": "The filings-intelligence plane is not enabled.",
  "requestId": "req_2ZK8Q1W9F4M6P7R3",
  "details": {}
}
```
</ResponseExample>

## Give this prompt to your agent

<Prompt>
GET /v1/filings/events is currently unavailable and returns `404 filings_intelligence_not_available`. Do not use it as an active monitoring dependency. When it becomes available, use the documented form, category, issuer, and filed-date filters to create a bounded review queue, and verify material events in the cited filing.
</Prompt>

## Failure posture

- treat `404 filings_intelligence_not_available` as the current endpoint state, not an empty result
- do not retry this endpoint as an active dependency until filings intelligence is enabled
- preserve `requestId` and `traceparent` in logs and downstream reports
