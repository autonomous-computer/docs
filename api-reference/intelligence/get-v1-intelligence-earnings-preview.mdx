---
title: "GET /v1/intelligence/earnings-preview"
description: "Return an earnings preview bundle with filing-derived drivers, risks, and traceable supporting context"
---

Return an earnings preview bundle with filing-derived drivers, risks, and traceable supporting context.

<Info>
Audience: application and coding agent. This public, filing-derived endpoint does not return TIKR consensus estimates or expose the internal-only consensus estimates plane.
</Info>

## Canonical metadata

- `requestId`
- `traceparent`

## Example request

<RequestExample>
```bash
curl -X GET \
  -H "x-api-key: $SECAPI_API_KEY" \
  -H "secapi-version: 2026-03-19" \
  "https://api.secapi.ai/v1/intelligence/earnings-preview?ticker=AMD"
```
</RequestExample>

## Example response

<ResponseExample>
```json
{
  "requestId": "req_2ZK8Q1W9F4M6P7R3"
}
```
</ResponseExample>

## Give this prompt to your agent

<Prompt>
Use SEC API GET /v1/intelligence/earnings-preview to return a filing-derived earnings preview with drivers, risks, and traceable supporting context. Preserve `requestId` and `traceparent` in the output. Return a concise markdown summary plus the exact structured payload fields a downstream engineer or agent should retain.
</Prompt>

## Failure posture

- Treat non-2xx responses as contract-aware failures, not free-form errors.
- Preserve `requestId` and `traceparent` in logs and downstream reports.
- If provenance or freshness metadata is present, return it unchanged so trust is not lost in the handoff.
