---
title: "GET /v1/intelligence/coverage"
description: "Filing-intelligence coverage endpoint (currently unavailable)"
---

Filing-intelligence coverage endpoint (currently unavailable)

<Info>
Audience: application and coding agent.
</Info>

## Coverage and source scope

This endpoint is currently unavailable and returns `404 filings_intelligence_not_available`. When available, it will compose a single-issuer rollup over persisted filings-intelligence stores, including tagged events, guidance, transcripts, and footnote newness. Coverage will reflect available extracted records, not complete issuer disclosure history.

## Current availability

This endpoint currently returns `404 filings_intelligence_not_available`. It is documented here so clients can prepare for the contract, but it is not available as an active coverage-report source. When it becomes available, preserve source counts, citation fields, and request metadata, then verify coverage against the underlying filings.

## Canonical metadata

- `requestId`
- `traceparent`

## Example request

<RequestExample>
```bash
curl -X GET \
  -H "x-api-key: $SECAPI_API_KEY" \
  -H "secapi-version: 2026-03-19" \
  "https://api.secapi.ai/v1/intelligence/coverage"
```
</RequestExample>

## Example response

<ResponseExample>
```json
{
  "requestId": "req_2ZK8Q1W9F4M6P7R3"
}
```
</ResponseExample>

## Give this prompt to your agent

<Prompt>
GET /v1/intelligence/coverage is currently unavailable and returns `404 filings_intelligence_not_available`. Do not use it as an active coverage-report dependency. When it becomes available, preserve source counts, citation fields, `requestId`, and `traceparent`, and verify issuer coverage against the underlying filing records.
</Prompt>

## Failure posture

- treat non-2xx responses as contract-aware failures, not free-form errors
- preserve `requestId` and `traceparent` in logs and downstream reports
- if provenance or freshness metadata is present, return it unchanged so trust is not lost in the handoff
