# GET /v1/dilution/reverse-splits

List reverse-stock-split actions with execution date and ratio (`split_from` : `split_to`). Ordered by `execution_date desc`. Verification block on every row.

<Info>
Audience: application and coding agent.
</Info>

## Canonical metadata

- `requestId`
- `traceparent`
- `verification`
- `provenance`

## Query parameters

| Param | Type | Description |
|---|---|---|
| `ticker` | string | Issuer ticker. |
| `execution_date_from` / `execution_date_to` | ISO date | Bounds on `execution_date`. |
| `cursor` | int | From prior page's `nextCursor`. |
| `limit` | int (1–50, default 10) | Page size. |
| `view` | enum | Response shape. |

## Example request

<RequestExample>
```bash
curl -X GET \
  -H "x-api-key: $OMNI_DATASTREAM_API_KEY" \
  -H "omni-version: 2026-03-19" \
  "https://api.secapi.ai/v1/dilution/reverse-splits?ticker=ACME"
```
</RequestExample>

## Example response

<ResponseExample>
```json
{
  "object": "list",
  "data": [
    {
      "object": "dilution_reverse_split",
      "id": "dil_rev_acme_2025q4",
      "ticker": "ACME",
      "executionDate": "2025-11-04",
      "splitFrom": 10,
      "splitTo": 1,
      "cik": "0001234567",
      "accessionNumber": "0001234567-25-006543",
      "filedAt": "2025-10-21T19:55:00.000Z",
      "verification": {
        "sourceSpan": "8-K#item-5.03#char[420..760]",
        "extractionTraceId": "lf_2025q4_acme_revsplit_extract",
        "confidence": 0.98,
        "crossValidations": [
          { "name": "ratio_consistent", "passed": true }
        ],
        "modelVersion": "dilution-v1.4",
        "verifiedAt": "2025-10-21T19:58:00.000Z"
      }
    }
  ],
  "hasMore": false,
  "nextCursor": null,
  "requestId": "req_example_123",
  "traceparent": "00-4bf92f3577b34da6a3ce929d0e0e4736-00f067aa0ba902b7-01"
}
```
</ResponseExample>

## Give this prompt to your agent

<Prompt>
Use Omni Datastream `GET /v1/dilution/reverse-splits` to surface historical reverse-split actions. Preserve `requestId`, `traceparent`, and `verification`. Report ratios as `splitFrom:splitTo` (e.g. `10:1`).
</Prompt>

## Failure posture

- treat non-2xx responses as contract-aware failures, not free-form errors
- preserve `requestId` and `traceparent`
