---
title: "Agent"
description: "Provision an agent's first organization-scoped API key through a short-lived sponsor-token exchange."
---

Provision an agent's first organization-scoped API key through a short-lived sponsor-token exchange.

An organization member first creates a [single-use sponsor token](/api-reference/agent/post-v1-agent-bootstrap-tokens), then the agent sends it to [bootstrap](/api-reference/agent/post-v1-agent-bootstrap). Bootstrap is intentionally pre-authenticated, and the issued API-key secret is returned once; do not log it.

<Note>
The sidebar labels inside this family are the literal calls you make over HTTP. Descriptions stay in the body so the navigation stays sharp and scannable.
</Note>

## Operations in this family

<CardGroup cols={2}>
<Card title="POST /v1/agent/bootstrap" href="/api-reference/agent/post-v1-agent-bootstrap">Exchange a sponsor token for the first org-scoped API key, billing snapshot, limits, and MCP install metadata</Card>
<Card title="POST /v1/agent/bootstrap_tokens" href="/api-reference/agent/post-v1-agent-bootstrap-tokens">Issue a short-lived, single-use sponsor token for agent bootstrap under the current organization</Card>
</CardGroup>
