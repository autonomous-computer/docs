# POST /v1/agent/bootstrap

Exchange a sponsor token for the first org-scoped API key, billing snapshot, limits, and MCP install metadata

<Info>
Audience: application and coding agent.
</Info>

## Canonical metadata

- `requestId`
- `traceparent`

## Example request

<RequestExample>
```bash
curl -X POST \
  -H "x-api-key: $OMNI_DATASTREAM_API_KEY" \
  -H "omni-version: 2026-03-19" \
  -H "content-type: application/json" \
  -d '{}' \
  "https://api.secapi.ai/v1/agent/bootstrap"
```
</RequestExample>

## Example response

<ResponseExample>
```json
{
  "requestId": "req_example_123",
  "traceparent": "00-4bf92f3577b34da6a3ce929d0e0e4736-00f067aa0ba902b7-01",
  "apiKey": {
    "id": "key_boot_abc123",
    "key": "omni_dk_agent_xxxxxxxxxxxx",
    "name": "agent-bootstrap-auto",
    "createdAt": "2026-03-29T14:00:00.000Z"
  },
  "org": {
    "id": "org_def456",
    "name": "Acme Capital",
    "plan": "pay-as-you-go"
  },
  "billing": {
    "plan": "pay-as-you-go",
    "status": "active",
    "includedCalls": 500,
    "usedCalls": 0
  },
  "limits": {
    "rateLimit": "60/min",
    "concurrency": 5,
    "maxBatchSize": 50
  },
  "mcp": {
    "endpoint": "https://api.secapi.ai/mcp",
    "transportType": "streamable-http",
    "installHint": "Add to your MCP config: {\"url\": \"https://api.secapi.ai/mcp\", \"headers\": {\"x-api-key\": \"<YOUR_KEY>\"}}"
  }
}
```
</ResponseExample>

## Give this prompt to your agent

<Prompt>
Use Omni Datastream POST /v1/agent/bootstrap to exchange a sponsor token for the first org-scoped api key, billing snapshot, limits, and mcp install metadata. Preserve `requestId`, `traceparent` in the output. Return a concise markdown summary plus the exact structured payload fields that a downstream engineer or agent should keep using this result.
</Prompt>

## Failure posture

- treat non-2xx responses as contract-aware failures, not free-form errors
- preserve `requestId` and `traceparent` in logs and downstream reports
- if provenance or freshness metadata is present, return it unchanged so trust is not lost in the handoff


