# GET /v1/facts

Return normalized SEC company facts for an issuer, concept, and optional form or unit. Defaults to the us-gaap taxonomy (10-K/10-Q); when no explicit taxonomy or form is supplied and the us-gaap lookup is empty, automatically falls back to ifrs-full on 20-F/6-K for foreign private issuers (e.g. SAP, TM, SONY, RIO).

<Info>
Audience: application and coding agent.
</Info>

## Agent mode

Pass `?view=agent` to receive the compact agent shape: concept + value + unit + period fields + lifted `accessionNumber` / `filingUrl` from provenance. Metadata envelopes are dropped. See [api conventions → response formats](/api-conventions) for the full `?view=` contract.

## Canonical metadata

- `requestId`
- `traceparent`
- `provenance`
- `freshness`
- `materialization`

## Example request

<RequestExample>
```bash
curl -X GET \
  -H "x-api-key: $SECAPI_API_KEY" \
  -H "secapi-version: 2026-03-19" \
  "https://api.secapi.ai/v1/facts?ticker=AAPL&tag=Revenues&period=quarter&limit=4&view=agent"
```
</RequestExample>

## Query parameters

- `ticker`, `symbol`, or `cik` identifies the issuer. `symbol` is accepted as an alias for `ticker`.
- `tag` is the XBRL concept for concept-specific lookups, such as `Revenues`, `Revenue`, `NetIncomeLoss`, or `PropertyPlantAndEquipmentNet`. If omitted, the API returns common financial metrics.
- `period=annual` returns annual facts. `period=quarterly`, `period=quarter`, and `period=q` return quarterly facts.
- `taxonomy`, `unit`, and `form` pin the lookup when you do not want the default us-gaap plus foreign-private-issuer fallback.
- `fy`, `year`, `fy_from`, `fyFrom`, `fy_to`, and `fyTo` bound fiscal years.
- `limit` controls the maximum number of fact points returned, up to 50.
- `include=geographic_segments` adds bounded revenue geography when available. Use `segment_limit` and `submission_file_limit` to control the enrichment budget.
- `view=agent` returns token-efficient fact rows with filing pointers lifted onto each item.

## Example response

<ResponseExample>
```json
{
  "object": "list",
  "data": [
    {
      "object": "fact_point",
      "tag": "Revenues",
      "label": "Revenue",
      "taxonomy": "us-gaap",
      "unit": "USD",
      "value": 95359000000,
      "periodEnd": "2025-06-28",
      "fy": 2025,
      "fp": "Q3",
      "form": "10-Q",
      "accessionNumber": "0000320193-25-000079",
      "filingUrl": "https://www.sec.gov/Archives/edgar/data/320193/000032019325000079/aapl-20250628.htm"
    }
  ],
  "hasMore": false,
  "nextCursor": null,
  "requestId": "req_2ZK8Q1W9F4M6P7R3"
}
```
</ResponseExample>

## Give this prompt to your agent

<Prompt>
Use SEC API GET /v1/facts with `ticker`, `symbol`, or `cik`, an XBRL `tag` for concept-specific lookups, and `period=annual|quarterly|quarter|q` to return normalized SEC company facts. Use `view=agent` for compact fact rows with `accessionNumber` and `filingUrl`; use `include=geographic_segments` only when revenue geography is needed. Preserve `requestId` and any provenance/freshness/materialization metadata present in default responses.
</Prompt>

## Failure posture

- treat non-2xx responses as contract-aware failures, not free-form errors
- preserve `requestId` and `traceparent` in logs and downstream reports
- retry with `ticker`, `symbol`, or `cik`, and add a valid `tag` when you need one specific concept
- invalid `period`, fiscal-year, or `limit` filters return `400 invalid_query_parameter`
- if provenance or freshness metadata is present, return it unchanged so trust is not lost in the handoff
