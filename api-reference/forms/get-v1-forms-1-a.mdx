---
title: "GET /v1/forms/1-a"
description: "Return recent Regulation A Form 1-A offering filings with cursor pagination and date filters"
---

Return recent Regulation A Form 1-A offering filings with cursor pagination and date filters

<Info>
Audience: application and coding agent.
</Info>

## Coverage and source scope

Use this route to find filed Regulation A Form 1-A offering statements. A Form 1-A filing is a disclosure record, not confirmation that the SEC qualified an offering, that a sale occurred, or that the full raise was completed. `date_from` and `date_to` bound filing dates. `submission_file_limit` can reduce the SEC submission history searched, so lower values can omit older records. Follow `nextCursor` with the same filters and review the cited filing for offering-stage context. See [filing conventions](/api-conventions).

## Canonical metadata

- `requestId`
- `traceparent`

## Example request

<RequestExample>
```bash
curl -X GET \
  -H "x-api-key: $SECAPI_API_KEY" \
  -H "secapi-version: 2026-03-19" \
  "https://api.secapi.ai/v1/forms/1-a"
```
</RequestExample>

## Example response

<ResponseExample>
```json
{
  "requestId": "req_2ZK8Q1W9F4M6P7R3"
}
```
</ResponseExample>

## Give this prompt to your agent

<Prompt>
Use SEC API GET /v1/forms/1-a to find filed Regulation A Form 1-A offering statements. Start with a ticker or CIK when known and use `date_from` and `date_to` as filing-date bounds. Preserve the accession number, filing date, filing URL, `hasMore`, `nextCursor`, `requestId`, and `traceparent`; when present, preserve `provenance`, `freshness`, and `materialization` metadata unchanged, but do not assume those fields on response shapes that omit them. A filing is not confirmation that an offering was qualified, sold, or fully raised. Keep the same filters while following `nextCursor`, and note that a lower `submission_file_limit` can omit older SEC matches.
</Prompt>

## Failure posture

- treat non-2xx responses as contract-aware failures, not free-form errors
- preserve `requestId` and `traceparent` in logs and downstream reports
- if provenance or freshness metadata is present, return it unchanged so trust is not lost in the handoff
