---
title: "GET /v1/events/ipo"
description: "Find filing-derived IPO and public-offering candidates from registration filings"
---

Find filing-derived IPO and public-offering candidates from registration filings

<Info>
Audience: application and coding agent.
</Info>

## Coverage and source scope

Finds IPO and public-offering candidates inferred from S-1, F-1, and 424B filings; it does not confirm that an offering priced, closed, or began trading. Treat filing dates as disclosure dates, preserve accession and source fields, and continue with the returned `nextCursor` value for the same filters. The supported forms and bounded submission scan define coverage, so no result is not proof that no offering occurred. See [API conventions](https://docs.secapi.ai/api-conventions).

## Canonical metadata

- `requestId`
- `traceparent`
- `per-row provenance`
- `per-row freshness`
- `per-row materialization`

## Example request

<RequestExample>
```bash
curl -X GET \
  -H "x-api-key: $SECAPI_API_KEY" \
  -H "secapi-version: 2026-03-19" \
  "https://api.secapi.ai/v1/events/ipo?ticker=AAPL&limit=10"
```
</RequestExample>

## Example response

<ResponseExample>
```json
{
  "object": "list",
  "data": [],
  "hasMore": false,
  "nextCursor": null,
  "requestId": "req_2ZK8Q1W9F4M6P7R3"
}
```
</ResponseExample>

## Give this prompt to your agent

<Prompt>
Use SEC API GET /v1/events/ipo to find filing-derived IPO and public-offering candidates from registration filings. Preserve `requestId`, `traceparent`, `per-row provenance`, `per-row freshness`, `per-row materialization` in the output. Return a concise markdown summary plus the exact structured payload fields that a downstream engineer or agent should keep using this result.
</Prompt>

## Failure posture

- treat non-2xx responses as contract-aware failures, not free-form errors
- preserve `requestId` and `traceparent` in logs and downstream reports
- if provenance or freshness metadata is present, return it unchanged so trust is not lost in the handoff
