# GET /v1/dashboard/overview

Return the logged-in dashboard overview with principal, org, enriched billing, usage, and API key context

<Info>
Audience: application and coding agent.
</Info>

## Canonical metadata

- `requestId`
- `traceparent`

## Example request

<RequestExample>
```bash
curl -X GET \
  -H "x-api-key: $OMNI_DATASTREAM_API_KEY" \
  -H "omni-version: 2026-03-19" \
  "https://api.secapi.ai/v1/dashboard/overview"
```
</RequestExample>

## Example response

<ResponseExample>
```json
{
  "requestId": "req_example_123",
  "traceparent": "00-4bf92f3577b34da6a3ce929d0e0e4736-00f067aa0ba902b7-01",
  "principal": {
    "id": "usr_abc123",
    "email": "analyst@example.com",
    "name": "Jane Doe"
  },
  "org": {
    "id": "org_def456",
    "name": "Acme Capital",
    "plan": "team",
    "createdAt": "2025-11-01T00:00:00.000Z"
  },
  "billing": {
    "plan": "team",
    "status": "active",
    "currentPeriodEnd": "2026-04-01T00:00:00.000Z",
    "includedCalls": 10000,
    "usedCalls": 3847,
    "paygEnabled": true
  },
  "usage": {
    "last30d": {
      "totalRequests": 3847,
      "meterBreakdown": {
        "light_reads": 1200,
        "standard_reads": 1800,
        "heavy_extracts": 347,
        "intelligence_queries": 450,
        "market_data_reads": 50
      }
    }
  },
  "apiKeys": [
    {
      "id": "key_ghi789",
      "name": "Production",
      "prefix": "omni_dk_prod_****",
      "createdAt": "2025-11-02T00:00:00.000Z",
      "lastUsedAt": "2026-03-29T12:34:56.000Z"
    }
  ]
}
```
</ResponseExample>

## Give this prompt to your agent

<Prompt>
Use Omni Datastream GET /v1/dashboard/overview to return the logged-in dashboard overview with principal, org, enriched billing, usage, and api key context. Preserve `requestId`, `traceparent` in the output. Return a concise markdown summary plus the exact structured payload fields that a downstream engineer or agent should keep using this result.
</Prompt>

## Failure posture

- treat non-2xx responses as contract-aware failures, not free-form errors
- preserve `requestId` and `traceparent` in logs and downstream reports
- if provenance or freshness metadata is present, return it unchanged so trust is not lost in the handoff


