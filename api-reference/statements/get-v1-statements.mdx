# GET /v1/statements

Return a normalized statement bundle backed by SEC company facts

<Info>
Use this endpoint from server-side applications, SDKs, or agent workflows.
</Info>

## Agent mode

Pass `?view=agent` to receive the compact agent shape: rows + periods with tag/value/period metadata. Provenance, completeness, freshness, materialization, and validation envelopes are dropped. See [api conventions → response formats](/api-conventions) for the full `?view=` contract.

## Response metadata

- `requestId`
- `traceparent`
- `provenance`
- `freshness`
- `materialization`

## Example request

<RequestExample>
```bash
curl -X GET \
  -H "x-api-key: $SECAPI_API_KEY" \
  -H "secapi-version: 2026-03-19" \
  "https://api.secapi.ai/v1/statements?statement=income_statement&symbol=AAPL&period=quarter"
```
</RequestExample>

## Query parameters

- `ticker`, `symbol`, or `cik` identifies the issuer. `symbol` is accepted as an alias for `ticker`.
- `statement` selects the statement key, such as `income_statement`, `balance_sheet`, or `cash_flow_statement`.
- `fy`, `year`, `fy_from`, `fyFrom`, `fy_to`, and `fyTo` bound fiscal years.
- `limit` caps periods returned, up to `40`.
- `view=agent` returns compact rows and periods for agent workflows; `view=compact` returns period-indexed row arrays.

## Period aliases

Use `period=annual` for annual statements or `period=quarterly` for quarterly statements. The shorter `period=quarter` and `period=q` aliases are also accepted for quarterly data.

## Example response

<ResponseExample>
```json
{
  "requestId": "req_example_123",
  "traceparent": "00-4bf92f3577b34da6a3ce929d0e0e4736-00f067aa0ba902b7-01",
  "provenance": {
    "source": "sec",
    "accessionNumber": "0000320193-25-000079",
    "filingUrl": "https://www.sec.gov/Archives/edgar/data/320193/000032019325000079/aapl-20250927_htm.xml",
    "retrievedAt": "2026-03-17T00:00:00.000Z",
    "parserVersion": "2026-03-13.5",
    "rawSourceKey": "0000320193-25-000079"
  },
  "freshness": {
    "status": "fresh",
    "asOf": "2026-03-17T00:00:00.000Z",
    "sourcePublishedAt": "2025-10-31T00:00:00.000Z",
    "lagMs": 900000
  },
  "materialization": {
    "parserVersion": "2026-03-13.5",
    "materializationVersion": "sec-api-3.0"
  }
}
```
</ResponseExample>

## Give this prompt to your agent

<Prompt description="Copy this SEC API prompt for your agent.">
Use SEC API GET /v1/statements with `ticker`, `symbol`, or `cik` to return a normalized statement bundle backed by SEC company facts. Preserve `requestId`, `traceparent`, `provenance`, `freshness`, `materialization` in the output. Keep the response fields your application uses for follow-up requests.
</Prompt>

## Errors and partial results

- Treat non-2xx responses as API errors and log the response body for debugging
- Log `requestId` and `traceparent` so SEC API support can trace the request
- if provenance or freshness metadata is present, return it unchanged so trust is not lost in the handoff
