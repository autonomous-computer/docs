---
title: "Webhooks API"
description: "Manage signed webhook endpoints, delivery history, replay, and secret rotation."
---

# Webhooks API

<Info>
Manage signed webhook endpoints, delivery history, replay, and secret rotation.
</Info>

## Use This API To

- List and create webhook endpoints.
- Inspect delivery attempts and replay individual deliveries.
- Rotate signing secrets for webhook endpoints.

## Before You Call

<Steps>
  <Step title="Verify signatures on every inbound webhook">
    Verify signatures on every inbound webhook.
  </Step>
  <Step title="Use replay for idempotent recovery, not duplicate business actions">
    Use replay for idempotent recovery, not duplicate business actions.
  </Step>
  <Step title="Store webhook endpoint IDs and event IDs for debugging">
    Store webhook endpoint IDs and event IDs for debugging.
  </Step>
</Steps>

## Endpoints

<CardGroup cols={2}>
<Card title="GET /v1/webhook_endpoints" icon="square-terminal" href="/api-reference/webhooks/get-v1-webhook-endpoints">List webhook endpoints for the current organization</Card>
<Card title="POST /v1/webhook_endpoints" icon="square-terminal" href="/api-reference/webhooks/post-v1-webhook-endpoints">Create a signed webhook endpoint for artifact and stream events</Card>
<Card title="GET /v1/webhook_endpoints/{webhook_id}/deliveries" icon="square-terminal" href="/api-reference/webhooks/get-v1-webhook-endpoints-webhook-id-deliveries">List canonical delivery attempts for a webhook endpoint</Card>
<Card title="POST /v1/webhook_endpoints/{webhook_id}/deliveries/{delivery_id}/replay" icon="square-terminal" href="/api-reference/webhooks/post-v1-webhook-endpoints-webhook-id-deliveries-delivery-id-replay">Replay a stored webhook delivery attempt by re-emitting its source event to the endpoint</Card>
<Card title="POST /v1/webhook_endpoints/{webhook_id}/rotate_secret" icon="square-terminal" href="/api-reference/webhooks/post-v1-webhook-endpoints-webhook-id-rotate-secret">Rotate the signing secret for a webhook endpoint</Card>
</CardGroup>
