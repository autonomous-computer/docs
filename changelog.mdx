# Changelog

## Discipline

- every externally visible ship gets a dated entry on the same day it lands in the release branch
- entries should describe the operator-visible outcome, not only the internal ticket title
- any compatibility change should link to the relevant migration guide and mention the pinned `omni-version` expectation
- benchmark, freshness, and coverage claims should only be copied from tracked source-of-truth artifacts

## 2026-03-16

- closed Omni Datastream `2.0` core execution
- shipped the manifest-first section extraction hot path and persisted section warming on cold misses
- closed the final live section benchmark gap versus `sec-api.io`:
  - `section_item_1a_aapl`: Omni `155.36ms` p50 vs `sec-api` `295.79ms` p50
- confirmed the production decision to keep `TS/Bun manifest-first` as the winning hot path while retaining Rust as a prototype lane only
- completed broad historical waves for the highest-value filing families:
  - `10-K`
  - `10-Q`
  - `8-K`
  - `3`
  - `4`
  - `5`
  - `13F`
- moved the live developer docs to `docs.secapi.ai` on Mintlify and shipped the refreshed docs IA
- shipped native hosted MCP install metadata for Claude Code, Cursor, and generic SSE clients
- shipped the publish-ready JavaScript SDK with typed error handling, package exports, docs, and MCP examples
- expanded docs IA with migration guides, benchmark/freshness communication, and auditable coverage pages
- broadened historical ownership and insider backfill support, including EDGAR-derived universe discovery, filing-level issuer resolution, and truthful provenance reporting
- shipped fast-follow fixes for statement alias normalization, ticker/cache normalization, replay retry hardening, and the PR 23 / PR 28 / PR 29 review lanes

## 2026-03-15

- integrated the competitive benchmarking and eval baseline lane into the canonical benchmark stack
- validated the 75-question API eval corpus against the live API and grounded the extraction truth against real filings
- added parser-risk fixtures and wired benchmark/eval outputs into the current validation path
- completed real agent-mode evaluation support with tool-use, current MCP tool definitions, and scoring/reporting that captures correctness, latency, tool count, and token usage

## 2026-03-16
- initial 2026-03-16 batch superseded by the final closeout entry above

## 2026-03-14

- launched `secapi.ai` and `api.secapi.ai`
- shipped live Stripe catalog, checkout, portal, and billing webhooks
- shipped live Typesense-backed search in production
- shipped WorkOS-backed MCP discovery endpoints
- shipped operator admin routes for tenant snapshots, request diagnostics, and delivery summaries
- hardened compensation parsing across Apple, Microsoft, and NVIDIA proxy filings

## 2026-03-13

- created the standalone `omni-datastream` repo and draft PR workflow
- shipped API, CLI, SDK, and hosted MCP skeletons
- shipped filing search, section extraction, facts, statements, 13F, insiders, compensation, artifacts, events, webhooks, and streams
- provisioned Cloudflare R2 buckets, Cloudflare edge, Railway runtime, PostHog, Sentry, Typesense, Stripe, and Dagster seams
