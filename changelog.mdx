# Changelog

<Info>
The changelog is part of the trust surface. It keeps shipped changes, release rhythm, and the why behind each update visible.
</Info>

## Publishing cadence

- The site changelog is regenerated from first-parent production history.
- The weekly automation runs each Sunday, regenerates the changelog, and opens a draft review PR before anything publishes.

## Release trail

<Update label="June 24, 2026" description="SDK 1.0.1 release train, public install checks, and mirror parity" tags={["SEC API", "Release"]}>
SEC API SDKs now share a stable 1.0.1 release line across JavaScript, Python, Go, Rust, and the CLI.

### Highlights

- Public packages are cut from mirror repos that are checked against the monorepo SDK source before publishing.
- Clean-room install smoke tests verify npm, PyPI, crates.io, and Go module consumers can import the public packages and compile docs-facing examples.
- A cross-SDK parity scoreboard tracks auth, retries, pagination, request IDs, typed errors, grouped namespaces, and agent workflow examples.
</Update>

<Update label="June 24, 2026" description="Prepare SDK 1.0.1 release train" tags={["SEC API", "Release"]}>
A focused product or platform improvement shipped to SEC API.

### Highlights

- Prepare SDK 1.0.1 release train
</Update>

<Update label="June 23, 2026" description="fix(api): improve response ergonomics + 2 more" tags={["SEC API", "Release"]}>
Highlights from this shipping window include fix(api): improve response ergonomics; Improve SDK env fallback ergonomics; feat(monitoring): add liveness dead-man heartbeat to clickhouse-freshness-monitor.

### Highlights

- fix(api): improve response ergonomics
- Improve SDK env fallback ergonomics
- feat(monitoring): add liveness dead-man heartbeat to clickhouse-freshness-monitor
</Update>

<Update label="June 22, 2026" description="fix(canary): stop false flaps on insider forms + raise page threshold + 5 more" tags={["SEC API", "Release"]}>
Highlights from this shipping window include fix(canary): stop false flaps on insider forms + raise page threshold; fix(ingestion): deploy edgar-rss-poller with the API (accepted_at NULL root cause); feat(api-keys): Stripe-style secapi_ key format + scrub ods_ + key UX.

### Highlights

- fix(canary): stop false flaps on insider forms + raise page threshold
- fix(ingestion): deploy edgar-rss-poller with the API (accepted_at NULL root cause)
- feat(api-keys): Stripe-style secapi_ key format + scrub ods_ + key UX
- fix(canary): /v1/filings list probe limit 100 -> 50 (API caps at 50)
</Update>

<Update label="June 21, 2026" description="feat(dashboard): toast system + skeleton loaders + 2 more" tags={["SEC API", "Release"]}>
Highlights from this shipping window include feat(dashboard): toast system + skeleton loaders; fix(onboarding): close North Star copy + workspace fallback gaps (post-#899 audit); OpenRouter-fidelity onboarding + app animation system + Stripe RAK/sandbox.

### Highlights

- feat(dashboard): toast system + skeleton loaders
- fix(onboarding): close North Star copy + workspace fallback gaps (post-#899 audit)
- OpenRouter-fidelity onboarding + app animation system + Stripe RAK/sandbox
</Update>

<Update label="June 20, 2026" description="infra(dms): auto-recover DR replication task on CDC-phase failure + alert  + 1 more" tags={["SEC API", "Release"]}>
Highlights from this shipping window include infra(dms): auto-recover DR replication task on CDC-phase failure + alert; feat: OpenRouter-style signup + onboarding — Phase 1 (self-hosted auth).

### Highlights

- infra(dms): auto-recover DR replication task on CDC-phase failure + alert
- feat: OpenRouter-style signup + onboarding — Phase 1 (self-hosted auth)
</Update>

<Update label="June 19, 2026" description="fix(api): order /v1/filings by accepted_at, not created_at (PR-B, maintenance update) + 3 more" tags={["SEC API", "Release"]}>
Highlights from this shipping window include fix(api): order /v1/filings by accepted_at, not created_at (PR-B, maintenance update); feat(db): materialize filing_manifests.accepted_at + ordering indexes + backfill (PR-A scaffolding); fix(canary): suppress stale_missing_from_list on EDGAR-quiet days (holiday/weekend).

### Highlights

- fix(api): order /v1/filings by accepted_at, not created_at (PR-B, maintenance update)
- feat(db): materialize filing_manifests.accepted_at + ordering indexes + backfill (PR-A scaffolding)
- fix(canary): suppress stale_missing_from_list on EDGAR-quiet days (holiday/weekend)
- perf(db): prod PG hardening from audit — pg_stat_statements, autovacuum, claim + FK indexes
</Update>

<Update label="June 18, 2026" description="fix(pipeline): dilution slice-keys check reads current-partition artifact, not cross-partition fs load  + 18 more" tags={["SEC API", "Release"]}>
Highlights from this shipping window include fix(pipeline): dilution slice-keys check reads current-partition artifact, not cross-partition fs load; feat(api): maintenance update + Segments sector validation → GA-readiness (beta flag staged, not flipped); feat(site): Cloudflare-fidelity visual reskin of the marketing site.

### Highlights

- fix(pipeline): dilution slice-keys check reads current-partition artifact, not cross-partition fs load
- feat(api): maintenance update + Segments sector validation → GA-readiness (beta flag staged, not flipped)
- feat(site): Cloudflare-fidelity visual reskin of the marketing site
- feat(factors): empirical factor-return quality calibration + GA recommendation
</Update>

<Update label="June 17, 2026" description="fix(factors): floor leveraged scaled_return at the purification producer (maintenance update follow-up) + 20 more" tags={["SEC API", "Release"]}>
Highlights from this shipping window include fix(factors): floor leveraged scaled_return at the purification producer (maintenance update follow-up); fix(api): correct stale nextCursor assertion in /v1/filings market-latest agent-view test; fix(api): redact platform/vendor provenance from customer /v1 responses.

### Highlights

- fix(factors): floor leveraged scaled_return at the purification producer (maintenance update follow-up)
- fix(api): correct stale nextCursor assertion in /v1/filings market-latest agent-view test
- fix(api): redact platform/vendor provenance from customer /v1 responses
- fix(ops): benchmark-docs formatDate throws on missing date instead of stamping now()
</Update>

<Update label="June 16, 2026" description="fix(canary): warm-up retry on /v1/filings/latest cold-call blip (stop false page) + 12 more" tags={["SEC API", "Release"]}>
Highlights from this shipping window include fix(canary): warm-up retry on /v1/filings/latest cold-call blip (stop false page); feat(ops): page on-call via BetterStack when a Dagster drain task OOMs; perf(drain): bump backfill drain concurrency 4 -> 6 (~3x) on xl.

### Highlights

- fix(canary): warm-up retry on /v1/filings/latest cold-call blip (stop false page)
- feat(ops): page on-call via BetterStack when a Dagster drain task OOMs
- perf(drain): bump backfill drain concurrency 4 -> 6 (~3x) on xl
- docs: accuracy pass — refresh-backed session model + signup wording
</Update>

<Update label="June 15, 2026" description="fix(auth): keep dashboard sessions alive — refresh-backed sealed cookies (30d/90d) + 14 more" tags={["SEC API", "Release"]}>
Highlights from this shipping window include fix(auth): keep dashboard sessions alive — refresh-backed sealed cookies (30d/90d); feat(legal): launch-readiness legal-docs hardening — ToS/Privacy audit, consent capture, new docs; feat(auth): adopt existing org by verified email to prevent duplicate orgs.

### Highlights

- fix(auth): keep dashboard sessions alive — refresh-backed sealed cookies (30d/90d)
- feat(legal): launch-readiness legal-docs hardening — ToS/Privacy audit, consent capture, new docs
- feat(auth): adopt existing org by verified email to prevent duplicate orgs
- feat(site): launch-readiness hardening — a11y, perf, SEO, functional, responsive
</Update>

<Update label="June 14, 2026" description="feat(ops): credential drift detector + rotation runbook (prevents stale-credential outages) + 2 more" tags={["SEC API", "Release"]}>
Highlights from this shipping window include feat(ops): credential drift detector + rotation runbook (prevents stale-credential outages); feat(ingest): fit drain budget to cron so passes stop overlap-skipping; fix(aws-origin): auto-propagate operator key to RDS + force ECS roll on secret change.

### Highlights

- feat(ops): credential drift detector + rotation runbook (prevents stale-credential outages)
- feat(ingest): fit drain budget to cron so passes stop overlap-skipping
- fix(aws-origin): auto-propagate operator key to RDS + force ECS roll on secret change
</Update>

<Update label="June 12, 2026" description="chore(factors): close archive evidence gaps" tags={["SEC API", "Release"]}>
A focused product or platform improvement shipped to SEC API.

### Highlights

- chore(factors): close archive evidence gaps
</Update>

<Update label="June 10, 2026" description="fix: use confidential WorkOS AuthKit exchange + 2 more" tags={["SEC API", "Release"]}>
Highlights from this shipping window include fix: use confidential WorkOS AuthKit exchange; fix: restore Railway private Postgres binding; maintenance update insider trade list cache.

### Highlights

- fix: use confidential WorkOS AuthKit exchange
- fix: restore Railway private Postgres binding
- maintenance update insider trade list cache
</Update>

<Update label="June 9, 2026" description="maintenance update fix 13F holder archive fallback + 4 more" tags={["SEC API", "Release"]}>
Highlights from this shipping window include maintenance update fix 13F holder archive fallback; maintenance update expose holder filing-date backfill; fix: accept workos user management smoke endpoints.

### Highlights

- maintenance update fix 13F holder archive fallback
- maintenance update expose holder filing-date backfill
- fix: accept workos user management smoke endpoints
- fix: extend railway migration deploy window
</Update>

<Update label="May 25, 2026" description="maintenance update harden Cloudflare maintenance and deploy failover" tags={["SEC API", "Release"]}>
A focused product or platform improvement shipped to SEC API.

### Highlights

- maintenance update harden Cloudflare maintenance and deploy failover
</Update>

<Update label="May 24, 2026" description="fix: allow weekend clickhouse freshness gaps + 2 more" tags={["SEC API", "Release"]}>
Highlights from this shipping window include fix: allow weekend clickhouse freshness gaps; fix: fail open factor purification subprocess; fix: package datastream api materializer fixtures.

### Highlights

- fix: allow weekend clickhouse freshness gaps
- fix: fail open factor purification subprocess
- fix: package datastream api materializer fixtures
</Update>

<Update label="May 23, 2026" description="fix(ops): auto-run Cloudflare maintenance recovery after deploys + 12 more" tags={["SEC API", "Release"]}>
Highlights from this shipping window include fix(ops): auto-run Cloudflare maintenance recovery after deploys; fix(ops): stabilize authenticated BetterStack monitor; fix(ops): recover stale Cloudflare maintenance and close ClickHouse evidence.

### Highlights

- fix(ops): auto-run Cloudflare maintenance recovery after deploys
- fix(ops): stabilize authenticated BetterStack monitor
- fix(ops): recover stale Cloudflare maintenance and close ClickHouse evidence
- Refresh enterprise launch gate evidence
</Update>

<Update label="May 22, 2026" description="maintenance update add enterprise drill evidence runbooks + 1 more" tags={["SEC API", "Release"]}>
Highlights from this shipping window include maintenance update add enterprise drill evidence runbooks; maintenance update require structured Phase 5 drill evidence.

### Highlights

- maintenance update add enterprise drill evidence runbooks
- maintenance update require structured Phase 5 drill evidence
</Update>

<Update label="May 22, 2026" description="Create Railway worker service without CLI link + 6 more" tags={["SEC API", "Release"]}>
Highlights from this shipping window include Create Railway worker service without CLI link; maintenance update fix DMS Postgres endpoint template; maintenance update fix provider degradation drills.

### Highlights

- Create Railway worker service without CLI link
- maintenance update fix DMS Postgres endpoint template
- maintenance update fix provider degradation drills
- maintenance update capture Phase 5 drill evidence gate
</Update>

<Update label="May 21, 2026" description="maintenance update add enterprise launch gate + 31 more" tags={["SEC API", "Release"]}>
Highlights from this shipping window include maintenance update add enterprise launch gate; maintenance update add vector embedding cache metrics; maintenance update add vector search degradation metadata.

### Highlights

- maintenance update add enterprise launch gate
- maintenance update add vector embedding cache metrics
- maintenance update add vector search degradation metadata
- maintenance update add Pinecone rebuild RTO audit
</Update>

<Update label="May 20, 2026" description="maintenance update add Dagster schedule repair workflow + 4 more" tags={["SEC API", "Release"]}>
Highlights from this shipping window include maintenance update add Dagster schedule repair workflow; feat(reliability): add origin protection header; feat(reliability): add R2 degraded fallback.

### Highlights

- maintenance update add Dagster schedule repair workflow
- feat(reliability): add origin protection header
- feat(reliability): add R2 degraded fallback
- feat(reliability): add origin failover contract
</Update>

<Update label="May 2, 2026" description="fix(api): harden rate-limit client identity" tags={["SEC API", "Release"]}>
A focused product or platform improvement shipped to SEC API.

### Highlights

- fix(api): harden rate-limit client identity
</Update>

<Update label="May 1, 2026" description="chore(ops): mirror pipeline-cost-telemetry + 1 more" tags={["SEC API", "Release"]}>
Highlights from this shipping window include chore(ops): mirror pipeline-cost-telemetry; chore(ops): mirror pipeline-cost-telemetry.

### Highlights

- chore(ops): mirror pipeline-cost-telemetry
</Update>

<Update label="April 30, 2026" description="fix(intraday): delete in-process TS worker, bump Dagster cadence to 5-min  + 5 more" tags={["SEC API", "Release"]}>
Highlights from this shipping window include fix(intraday): delete in-process TS worker, bump Dagster cadence to 5-min; chore(stripe): close-out bundle - webhook secret + audit DB reconciliation (maintenance update + 3403); chore(security): SAST audit close-out — open-redirect + GHA shell-injection (maintenance update + maintenance update).

### Highlights

- fix(intraday): delete in-process TS worker, bump Dagster cadence to 5-min
- chore(stripe): close-out bundle - webhook secret + audit DB reconciliation (maintenance update + 3403)
- chore(security): SAST audit close-out — open-redirect + GHA shell-injection (maintenance update + maintenance update)
- feat(mcp): wire mcpToolName into ai_queries quota gate
</Update>

<Update label="April 29, 2026" description="fix(stripe): increase emission drain rate + add observability + backfill script  + 6 more" tags={["SEC API", "Release"]}>
Highlights from this shipping window include fix(stripe): increase emission drain rate + add observability + backfill script; feat(contracts): investor materialization payloads (maintenance updated); feat(contracts): replay + fullindex + historical payloads (maintenance updatec).

### Highlights

- fix(stripe): increase emission drain rate + add observability + backfill script
- feat(contracts): investor materialization payloads (maintenance updated)
- feat(contracts): replay + fullindex + historical payloads (maintenance updatec)
- feat(contracts): dilution payloads — Pydantic contracts for ops/extractions/dilution/* (maintenance updateb)
</Update>

<Update label="April 28, 2026" description="Revert &quot;Merge pull request #424 from autonomous-computer/danielscrivner/omni-3244-wire-automated-monitor-destination-dispatch-via-dagster-cron&quot;" tags={["SEC API", "Release"]}>
A focused product or platform improvement shipped to SEC API.

### Highlights

- Revert "Merge pull request #424 from autonomous-computer/danielscrivner/omni-3244-wire-automated-monitor-destination-dispatch-via-dagster-cron"
</Update>

<Update label="April 27, 2026" description="feat(bench): multi-ticker pool + variance_flag for maintenance update (numbers refresh deferred) + 1 more" tags={["SEC API", "Release"]}>
Highlights from this shipping window include feat(bench): multi-ticker pool + variance_flag for maintenance update (numbers refresh deferred); fix(ci): declare @aws-sdk/client-s3 at workspace root for Status Rollup.

### Highlights

- feat(bench): multi-ticker pool + variance_flag for maintenance update (numbers refresh deferred)
- fix(ci): declare @aws-sdk/client-s3 at workspace root for Status Rollup
</Update>

<Update label="April 26, 2026" description="Dilution endpoints in all four SDKs (TS / Python / Go / Rust) + secapi CLI" tags={["SEC API", "Release"]}>
All 14 dilution endpoints — events, warrants, convertibles, ROFR, lockups, cash position, corporate actions, Nasdaq compliance, ratings, reverse splits, score, share-float history, coverage — are now first-class methods in every SDK and the secapi CLI. Wedge feature, fully reachable.

### Highlights

- TypeScript SDK: 14 new methods on SecApiClient (dilutionEvents, dilutionEventDetail, dilutionWarrants, dilutionConvertibles, dilutionRofr, dilutionLockups, dilutionCashPosition, dilutionCorporateActions, dilutionNasdaqCompliance, dilutionRatings, dilutionReverseSplits, dilutionScore, dilutionShareFloatHistory, dilutionCoverage); bumped to 0.2.0
- Python SDK: matching client.dilution_* methods (with an is_atm boolean adapter so True/False serialize as the lowercase strings the route expects); bumped to 0.2.0
- Go SDK: matching Client.Dilution* receiver methods returning (map[string]any, error); bumped to 0.2.0
- Rust SDK: matching client.dilution_* async functions returning Result<Value, SecApiError>; bumped to 0.2.0
- secapi CLI: new dilution group with 14 subcommands (secapi dilution events|event|warrants|convertibles|rofr|lockups|cash-position|corporate-actions|nasdaq-compliance|ratings|reverse-splits|score|share-float-history|coverage); bumped to 0.2.0
- ?view=agent honored across 13 of 14 endpoints (coverage is already a small rollup with no agent shape)
- Demo: secapi dilution ratings --overall-risk high --limit 10 returns the highest-dilution-risk issuers; secapi dilution score --ticker BBBB returns the per-issuer Dilution Score
</Update>

<Update label="April 25, 2026" description="fix(verification): drop truncated citation candidates for fractional values (maintenance update fast-follow)" tags={["SEC API", "Release"]}>
A focused product or platform improvement shipped to SEC API.

### Highlights

- fix(verification): drop truncated citation candidates for fractional values (maintenance update fast-follow)
</Update>

<Update label="April 24, 2026" description="Structured 8-K Item 5.07 voting results — first-in-market parser" tags={["SEC API", "Release"]}>
New GET /v1/events/voting-results endpoint and events.voting_results MCP tool return structured voting results from 8-K Item 5.07 filings with per-proposal vote counts, approval outcomes, and proposal-type classification. sec-api.io has had an open bug on 5.07 parsing since July 2025 — SEC API ships the structured route first.

### Highlights

- New: GET /v1/events/voting-results with ticker, cik, date range, meeting_type (annual|special), and cursor pagination
- New: MCP tool events.voting_results bootstraps the events.* namespace
- Structured proposals with votesFor, votesAgainst, abstain, brokerNonVotes, approved outcome, and proposal_type classification (elect_directors, ratify_auditor, say_on_pay, say_on_pay_frequency, approve_merger, shareholder_proposal, and more)
- parsing_confidence: high | medium | low — low-confidence events preserve the raw 5.07 summary and emit a Sentry breadcrumb so precision regressions are observable
- Vote counts are integers; null is semantically distinct from zero (null = not reported; zero = explicit zero votes)
- First in market — sec-api.io has had this parser broken since July 2025
</Update>

<Update label="April 23, 2026" description="decision(auth): API keys are the primary authentication method; OAuth (WorkOS) deferred to enterprise tier roadmap for 2026 Q3+" tags={["SEC API", "Release"]}>
Product-direction decision published for customers and partners. API keys cover every REST, SDK, CLI, and dashboard client today; OAuth/SSO via WorkOS is planned for the enterprise tier in 2026 Q3+.

### Highlights

- API keys are the primary authentication method for all plans
- OAuth/SSO via WorkOS is planned for the enterprise tier in 2026 Q3+
- Hosted MCP continues to use WorkOS OAuth discovery for the MCP surface
</Update>

<Update label="April 22, 2026" description="docs/ops(fast-follow): address bot findings on PR #363" tags={["SEC API", "Release"]}>
A focused product or platform improvement shipped to SEC API.

### Highlights

- docs/ops(fast-follow): address bot findings on PR #363
</Update>

<Update label="April 20, 2026" description="chore(deps): bump dagster-cloud-action from 1.13.0 to 1.13.1" tags={["SEC API", "Release"]}>
A focused product or platform improvement shipped to SEC API.

### Highlights

- chore(deps): bump dagster-io/dagster-cloud-action from 1.13.0 to 1.13.1 in the actions-all group
</Update>

<Update label="April 19, 2026" description="fix(ingest-worker): default concurrency back to 1 after #350 regression" tags={["SEC API", "Release"]}>
A focused product or platform improvement shipped to SEC API.

### Highlights

- fix(ingest-worker): default concurrency back to 1 after #350 regression
</Update>

<Update label="April 18, 2026" description="fix(sec-pipeline): suppress benign Dagster restart signals in Sentry" tags={["SEC API", "Release"]}>
A focused product or platform improvement shipped to SEC API.

### Highlights

- fix(sec-pipeline): suppress benign Dagster restart signals in Sentry
</Update>

<Update label="April 17, 2026" description="perf(xbrl, ingest-worker): parallelize hot loops + incremental upsert + 3 more" tags={["SEC API", "Release"]}>
Highlights from this shipping window include perf(xbrl): cache permanent-miss verdict for non-filer tickers; perf(xbrl): incremental upsert — ON CONFLICT DO UPDATE WHERE changed; perf(ingest-worker): parallelize claimed-item loop (sec_freshness_daily 440min → target 75-110min).

### Highlights

- perf(ingest-worker): parallelize claimed-item loop (sec_freshness_daily 440min → target 75-110min)
- perf(xbrl): incremental upsert — ON CONFLICT DO UPDATE WHERE changed (follow-up C)
- perf(xbrl): cache permanent-miss verdict for non-filer tickers (follow-up A)
- fix(ingest-worker): default concurrency back to 1 after PR #350 regression
</Update>

<Update label="April 16, 2026" description="fix(xbrl, factor-intraday, ingest-worker): classify permanent misses + surface failures + 4 more" tags={["SEC API", "Release"]}>
Highlights from this shipping window include fix(xbrl): classify permanent misses separately from real errors; fix(ingest-worker): don't kill the pass when a status-update DB call fails; fix(factor-intraday): surface failure details in Sentry + admin response.

### Highlights

- fix(xbrl): classify permanent misses separately from real errors
- fix(ingest-worker): don't kill the pass when a status-update DB call fails
- fix(factor-intraday): surface failure details in Sentry + admin response
- perf(xbrl): skip DELETE+INSERT on unchanged tickers (content-hash short-circuit)
</Update>

<Update label="April 15, 2026" description="fix: include infra/env in Railway deploy (fixes production crash loop)" tags={["SEC API", "Release"]}>
A focused product or platform improvement shipped to SEC API.

### Highlights

- fix: include infra/env in Railway deploy (fixes production crash loop)
</Update>

<Update label="April 14, 2026" description="feat: add 16 curated thematic baskets + FT parity map + 2 more" tags={["SEC API", "Release"]}>
Highlights from this shipping window include feat: add 16 curated thematic baskets + FT parity map + methodology docs; feat: add ~70 thematic stock basket factors for FT parity; platform Add KOSIS/ECOS API keys to env registry + Dagster provisioning.

### Highlights

- feat: add 16 curated thematic baskets + FT parity map + methodology docs
- feat: add ~70 thematic stock basket factors for FT parity
- platform Add KOSIS/ECOS API keys to env registry + Dagster provisioning
</Update>

<Update label="April 13, 2026" description="fix: fast-follow fixes for joint regression bot review findings + 1 more" tags={["SEC API", "Release"]}>
Highlights from this shipping window include fix: fast-follow fixes for joint regression bot review findings; fix stale BR UNRATE DBnomics alias frequency.

### Highlights

- fix: fast-follow fixes for joint regression bot review findings
- fix stale BR UNRATE DBnomics alias frequency
</Update>

<Update label="April 12, 2026" description="maintenance update fix production macro workflow" tags={["SEC API", "Release"]}>
A focused product or platform improvement shipped to SEC API.

### Highlights

- maintenance update fix production macro workflow
</Update>

<Update label="April 11, 2026" description="maintenance update official macro source sync + 1 more" tags={["SEC API", "Release"]}>
Highlights from this shipping window include maintenance update official macro source sync; maintenance update assets.

### Highlights

- maintenance update official macro source sync
- maintenance update assets
</Update>

<Update label="April 10, 2026" description="feat: install 4 launch skills into.claude/skills/ for auto-discovery" tags={["SEC API", "Release"]}>
A focused product or platform improvement shipped to SEC API.

### Highlights

- feat: install 4 launch skills into.claude/skills/ for auto-discovery
</Update>

<Update label="April 9, 2026" description="fix: return undefined for invalid year input instead of silently defaulting to 2000 (fast-follow from PR #231 bot review) + 3 more" tags={["SEC API", "Release"]}>
Highlights from this shipping window include fix: return undefined for invalid year input instead of silently defaulting to 2000 (fast-follow from PR #231 bot review); fix: remove dead rerankEnvDefault and shouldRerank variables (fast-follow from PR #232 bot review); fix: output webhook secret to stderr in CI where.secret file is lost (fast-follow from PR #251 bot review).

### Highlights

- fix: return undefined for invalid year input instead of silently defaulting to 2000 (fast-follow from PR #231 bot review)
- fix: remove dead rerankEnvDefault and shouldRerank variables (fast-follow from PR #232 bot review)
- fix: output webhook secret to stderr in CI where.secret file is lost (fast-follow from PR #251 bot review)
- fix: propagate filingDate to Pinecone metadata in fullindex backfill (fast-follow from PR #279 bot review)
</Update>

<Update label="April 8, 2026" description="fix: remove hot-lane steps from canonical freshness job + 1 more" tags={["SEC API", "Release"]}>
Highlights from this shipping window include fix: remove hot-lane steps from canonical freshness job; fix: restore legacy fullindex variant filtering.

### Highlights

- fix: remove hot-lane steps from canonical freshness job
- fix: restore legacy fullindex variant filtering
</Update>

<Update label="April 7, 2026" description="fix: increase ECS run resources to 4 vCPU / 16GB for fullindex render" tags={["SEC API", "Release"]}>
A focused product or platform improvement shipped to SEC API.

### Highlights

- fix: increase ECS run resources to 4 vCPU / 16GB for fullindex render
</Update>

<Update label="April 6, 2026" description="fix: prevent interleaveMerge from exceeding odd limit values" tags={["SEC API", "Release"]}>
A focused product or platform improvement shipped to SEC API.

### Highlights

- fix: prevent interleaveMerge from exceeding odd limit values
</Update>

<Update label="April 5, 2026" description="fix: add DNS preflight check before launching investor bun subprocesses + 2 more" tags={["SEC API", "Release"]}>
Highlights from this shipping window include fix: add DNS preflight check before launching investor bun subprocesses; fix: remove step-level RetryPolicy and reduce DNS retry overhead; fix: use persisted snapshots for standalone intraday materialization.

### Highlights

- fix: add DNS preflight check before launching investor bun subprocesses
- fix: remove step-level RetryPolicy and reduce DNS retry overhead
- fix: use persisted snapshots for standalone intraday materialization
</Update>

<Update label="April 4, 2026" description="fix: retry Dagster job launches on DNS failure to get fresh ECS tasks + 6 more" tags={["SEC API", "Release"]}>
Highlights from this shipping window include fix: retry Dagster job launches on DNS failure to get fresh ECS tasks; fix: harden extraction HTML stripping; deps(docker): bump python from 3.11-slim to 3.12-slim in /pipelines/sec-pipeline.

### Highlights

- fix: retry Dagster job launches on DNS failure to get fresh ECS tasks
- fix: harden extraction HTML stripping
- deps(docker): bump python from 3.11-slim to 3.12-slim in /pipelines/sec-pipeline
- deps(py): update certifi requirement in /autoresearch
</Update>

<Update label="April 3, 2026" description="Geographic segment depth detection + preferred stock deduction" tags={["SEC API", "Release"]}>
Segmented revenues now recognize geographic acronyms (EMEA, APAC, LACC) at depth 2 for proper isMostGranularSibling filtering. Company financials pipeline now deducts PreferredStockValue from StockholdersEquity when CommonStockholdersEquity is not filed separately.

### Highlights

- inferSegmentHierarchyDepth: added EMEA, APAC, LACC, Americas patterns
- PREFERRED_STOCK_CONCEPT_TAGS: PreferredStockValue, PreferredStockLiquidationPreference, PreferredStockCarryingAmount
- Common equity derivation: statement-level > supplemental > preferred stock deduction
</Update>

<Update label="April 2, 2026" description="fix: address 33 unresolved bot review comments from last 72h" tags={["SEC API", "Release"]}>
A focused product or platform improvement shipped to SEC API.

### Highlights

- fix: address 33 unresolved bot review comments from last 72h
</Update>

<Update label="April 1, 2026" description="fix: supplement sparse statement history + 9 more" tags={["SEC API", "Release"]}>
Highlights from this shipping window include fix: supplement sparse statement history; feat: expose stock repurchases in statements; fix: carry filing quarters through sec lookups.

### Highlights

- fix: supplement sparse statement history
- feat: expose stock repurchases in statements
- fix: carry filing quarters through sec lookups
- fix: reject xbrl noise in filing sections
</Update>

<Update label="March 31, 2026" description="fix: hide readiness bootstrap errors" tags={["SEC API", "Release"]}>
A focused product or platform improvement shipped to SEC API.

### Highlights

- fix: hide readiness bootstrap errors
</Update>

<Update label="March 29, 2026" description="fix: include evals/fixtures in Railway deploy (second missing module) + 2 more" tags={["SEC API", "Release"]}>
Highlights from this shipping window include fix: include evals/fixtures in Railway deploy (second missing module); fix: include infra/stripe in Railway deploy (fixes production crash loop); fix: reduce Sentry noise for expected errors.

### Highlights

- fix: include evals/fixtures in Railway deploy (second missing module)
- fix: include infra/stripe in Railway deploy (fixes production crash loop)
- fix: reduce Sentry noise for expected errors
</Update>

<Update label="March 27, 2026" description="bias section search to latest filing + 2 more" tags={["SEC API", "Release"]}>
Highlights from this shipping window include bias section search to latest filing; Remove FMP from company mapping audit; fix: add GHCR repository_credentials for Dagster Cloud ECS.

### Highlights

- bias section search to latest filing
- Remove FMP from company mapping audit
- fix: add GHCR repository_credentials for Dagster Cloud ECS
</Update>

<Update label="March 26, 2026" description="fix: regenerate OpenAPI spec and add Companies to Mintlify navigation + 2 more" tags={["SEC API", "Release"]}>
Highlights from this shipping window include fix: regenerate OpenAPI spec and add Companies to Mintlify navigation; fix: normalize generated openapi schema bounds; fix: improve financebench filing search paths.

### Highlights

- fix: regenerate OpenAPI spec and add Companies to Mintlify navigation
- fix: normalize generated openapi schema bounds
- fix: improve financebench filing search paths
</Update>

<Update label="March 25, 2026" description="Fast-follow intelligence materialization fixes" tags={["SEC API", "Release"]}>
A focused product or platform improvement shipped to SEC API.

### Highlights

- Fast-follow intelligence materialization fixes
</Update>

<Update label="March 24, 2026" description="Fix npm publish provenance and stale changelog baseline + 1 more" tags={["SEC API", "Release"]}>
Highlights from this shipping window include Fix npm publish provenance and stale changelog baseline; Fix npm publish provenance for private repo.

### Highlights

- Fix npm publish provenance and stale changelog baseline
- Fix npm publish provenance for private repo
</Update>

<Update label="March 23, 2026" description="chore: mark stripe live account dedicated + 8 more" tags={["SEC API", "Release"]}>
Highlights from this shipping window include chore: mark stripe live account dedicated; fix: authenticate final signoff factor check; chore: sync tracked stripe test state.

### Highlights

- chore: mark stripe live account dedicated
- fix: authenticate final signoff factor check
- chore: sync tracked stripe test state
- feat: add stripe state sync workflow
</Update>

<Update label="March 20, 2026" description="fix: align site browser smoke with local auth base + 1 more" tags={["SEC API", "Release"]}>
Highlights from this shipping window include fix: align site browser smoke with local auth base; fix: close auth rollout fast follows.

### Highlights

- fix: align site browser smoke with local auth base
- fix: close auth rollout fast follows
</Update>

<Update label="March 19, 2026" description="fix: fast-follow merged bugbot findings + 1 more" tags={["SEC API", "Release"]}>
Highlights from this shipping window include fix: fast-follow merged bugbot findings; Clarify entity fallback guard precedence.

### Highlights

- fix: fast-follow merged bugbot findings
- Clarify entity fallback guard precedence
</Update>

<Update label="March 18, 2026" description="fix: normalize company mapping backfill identifiers" tags={["SEC API", "Release"]}>
A focused product or platform improvement shipped to SEC API.

### Highlights

- fix: normalize company mapping backfill identifiers
</Update>

<Update label="March 16, 2026" description="fix: close PR 29 fast-follow bugs + 3 more" tags={["SEC API", "Release"]}>
Highlights from this shipping window include fix: close PR 29 fast-follow bugs; Ship ownership history backfill support; Relax stored filing manifest timestamp typing.

### Highlights

- fix: close PR 29 fast-follow bugs
- Ship ownership history backfill support
- Relax stored filing manifest timestamp typing
- Fix latest filing and section storage fast paths
</Update>

<Update label="March 15, 2026" description="Fix statement alias and cache normalization + 3 more" tags={["SEC API", "Release"]}>
Highlights from this shipping window include Fix statement alias and cache normalization; Harden SEC entity and replay handling; Port extraction autoresearch boundary win.

### Highlights

- Fix statement alias and cache normalization
- Harden SEC entity and replay handling
- Port extraction autoresearch boundary win
- Fix Python SDK parity and CI action versions
</Update>

<Update label="March 13, 2026" description="Initialize repository" tags={["SEC API", "Release"]}>
A focused product or platform improvement shipped to SEC API.

### Highlights

- Initialize repository
</Update>
