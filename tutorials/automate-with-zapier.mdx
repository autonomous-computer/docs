---
title: "SEC Data Automation with Zapier"
description: "Route signed SEC API monitor events through Zapier to a review log or notification."
---

Create a **Webhooks by Zapier -> Catch Hook** and use its generated HTTPS URL as
the destination. A spreadsheet row is a review queue, not the sole event ledger.

An organization administrator creates the public-HTTPS endpoint, subscribes it
to `monitor.match`, and creates the Form 4 monitor in the signed-in
[Delivery dashboard](https://secapi.ai/app/webhooks). This is a
human-authenticated control-plane workflow; do not use an API key for setup.

Keep the dashboard-issued endpoint and monitor IDs. Verify the raw-body
signature through a proxy when Zapier cannot safely do it. Use a dashboard test
event only to confirm the receiver and signature path: it sends `webhook.test`.
For the Zap's production review-row mapping, use this representative
`monitor.match` sample (or replay a recorded production `monitor.match`
delivery):

```json
{
  "object": "event",
  "id": "evt_example",
  "type": "monitor.match",
  "createdAt": "2026-07-14T00:00:00.000Z",
  "livemode": false,
  "requestId": "req_example",
  "data": {
    "monitor": { "id": "mon_example", "name": "Form 4 monitor" },
    "matches": [{
      "accessionNumber": "0000320193-26-000001",
      "htmlUrl": "https://www.sec.gov/Archives/edgar/data/320193/000032019326000001/example.htm",
      "form": "4",
      "ticker": "AAPL",
      "filingDate": "2026-07-14"
    }]
  }
}
```

Map the event ID, `data.monitor.id`, `data.matches[0].accessionNumber`,
`data.matches[0].htmlUrl`, `data.matches[0].form`,
`data.matches[0].ticker`, `data.matches[0].filingDate`, and receipt time into
the review row. The sample shows fields emitted for a keyword filing match;
retain the full received event rather than assuming a third-party parser shape.

Use an external unique key for event or accession-number deduplication. Inspect
delivery attempts and reconcile monitor matches: a missing Zap row is not proof
that no filing exists.

## Next steps

- [Build a Filing Monitor](/tutorials/build-filing-monitor)
- [Webhook endpoint reference](/api-reference/webhooks)
